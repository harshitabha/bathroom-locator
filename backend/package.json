{
  "name": "backend",
  "type": "module",
  "version": "1.0.0",
  "description": "Run the following command in the backend directory: \r ```\r docker compose up -d\r ```",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src test --no-inline-config --max-warnings 0",
    "test": "vitest run --no-file-parallelism --testTimeout=2000 --coverage",
    "start": "nodemon src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-openapi-validator": "^5.6.0",
    "pg": "^8.16.3",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "*",
    "eslint": "*",
    "eslint-config-google": "*",
    "eslint-plugin-jsdoc": "*", 
    "nodemon": "^3.1.10",
    "vitest": "*",
    "vite-plugin-eslint": "*"
  }
}
